# MOV y copias

Aísla `MOV` para mostrar carga de inmediatos y copia entre registros sin tocar memoria.

```forja-program
150: MOV r0, 5
151: MOV r1, r0
152: MOV r0, 9
153: MOV r2, r0
154: HALT
```

```forja-data
```