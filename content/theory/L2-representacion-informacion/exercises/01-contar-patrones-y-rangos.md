# Contar patrones y rangos

Este ejercicio fija la aritmética mínima del nivel: cuántos patrones distintos existen para un ancho dado y qué rangos aparecen cuando el mismo espacio finito se lee como unsigned o signed.

> Laboratorio: usá anchos de `8`, `16` y `32` bits y mirá cómo cambia el rango cuando alternás entre lectura unsigned y signed sin cambiar la cantidad total de bits.

## Consigna

Completá la tabla.

| Ancho | Patrones distintos | Rango unsigned | Rango signed |
|---|---|---|---|
| `8` bits | `?` | `?` | `?` |
| `16` bits | `?` | `?` | `?` |
| `32` bits | `?` | `?` | `?` |

La pregunta no es solo cuánto vale cada extremo, sino qué cambia y qué no cambia cuando el mismo ancho finito se interpreta de dos maneras distintas.